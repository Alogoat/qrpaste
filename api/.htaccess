# QRPaste API - Bezpečnost a Routing

# Povolit CORS pro specifické domény (nastav v produkci)
<IfModule mod_headers.c>
    Header set Access-Control-Allow-Origin "*"
    Header set Access-Control-Allow-Methods "GET, POST, OPTIONS"
    Header set Access-Control-Allow-Headers "Content-Type, Authorization"
</IfModule>

# Zabránění výpisu složky
Options -Indexes

# Bezpečnostní hlavičky
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# PHP nastavení
<IfModule mod_php.c>
    php_value upload_max_filesize 10M
    php_value post_max_size 10M
    php_value memory_limit 128M
    php_value max_execution_time 30
</IfModule>

# Komprese
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/plain
</IfModule>

# Cache pro statické soubory
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType application/json "access plus 0 seconds"
</IfModule>

# Rewrite rules pro čistší URL (volitelné)
# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     RewriteBase /api/
#     
#     # POST /save -> save.php
#     RewriteRule ^save/?$ save.php [L,QSA]
#     
#     # GET /get/:id -> get.php?id=:id
#     RewriteRule ^get/([a-zA-Z0-9]+)/?$ get.php?id=$1 [L,QSA]
#     
#     # GET /cleanup -> cleanup.php
#     RewriteRule ^cleanup/?$ cleanup.php [L,QSA]
# </IfModule>
